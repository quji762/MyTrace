<Window x:Class="Pulse.App.Spend.TokenSpendWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pulse — Token Spend"
        Width="760" Height="560"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header: provider tabs + summary line -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="ClaudeTab" Content="Claude Code" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="CodexTab" Content="Codex" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="OpenCodeTab" Content="OpenCode" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="CherryTab" Content="Cherry Studio" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="ClineTab" Content="Cline" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="AmpTab" Content="Amp" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="GooseTab" Content="Goose" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="CopilotTab" Content="Copilot OTEL" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="CopilotDesktopTab" Content="Copilot Desktop" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="CopilotVsCodeTab" Content="Copilot VS Code" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="KiroTab" Content="Kiro" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="QwenTab" Content="Qwen" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="GeminiTab" Content="Gemini" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="ZCodeTab" Content="ZCode" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="DshTab" Content="DSH" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="JunieTab" Content="Junie" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="CodebuffTab" Content="Codebuff" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="UnslothTab" Content="Unsloth" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="JcodeTab" Content="Jcode" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="FxTab" Content="Fx" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="OpenClawTab" Content="OpenClaw" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="DroidTab" Content="Droid" GroupName="spend" Margin="0,0,16,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="MuxTab" Content="Mux" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="GjcTab" Content="GJC" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="PiTab" Content="Pi" GroupName="spend" Margin="0,0,8,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
                <RadioButton x:Name="PrimeTab" Content="Prime Agent" GroupName="spend" Margin="0,0,16,0"
                             Foreground="#FFF2F2F2" Checked="OnProviderTabChanged" />
            </StackPanel>
            <Button Grid.Column="1" Content="Refresh" Padding="12,4" Click="OnRefresh" />
        </Grid>

        <TextBlock Grid.Row="1" x:Name="SummaryText" FontSize="12" Foreground="#FFF2F2F2"
                   Margin="0,10,0,8" TextWrapping="Wrap" />

        <!-- Daily tokens chart: one bar per day, gaps kept as zero-height days -->
        <Border Grid.Row="2" Background="#FF2A2A2A" CornerRadius="6" Padding="8">
            <Canvas x:Name="ChartCanvas" SizeChanged="OnChartResized" />
        </Border>

        <!-- Session list: one row per transcript -->
        <Border Grid.Row="3" Background="#FF2A2A2A" CornerRadius="6" Padding="8" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Sessions" FontSize="12" FontWeight="SemiBold"
                           Foreground="#FFF2F2F2" Margin="0,0,0,6" />
                <ListView Grid.Row="1" x:Name="SessionList" Background="Transparent"
                          BorderThickness="0" Foreground="#FFF2F2F2">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Session" Width="300"
                                            DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Project" Width="140"
                                            DisplayMemberBinding="{Binding Project}" />
                            <GridViewColumn Header="Tokens" Width="90"
                                            DisplayMemberBinding="{Binding TokensText}" />
                            <GridViewColumn Header="End" Width="140"
                                            DisplayMemberBinding="{Binding EndText}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Window>
