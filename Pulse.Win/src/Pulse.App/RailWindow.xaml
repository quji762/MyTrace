<Window x:Class="Pulse.App.RailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pulse"
        Width="120" Height="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        ShowActivated="False">
    <Border Background="{StaticResource RailBackground}"
            CornerRadius="12"
            Padding="10,14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Background="Transparent">
                <StackPanel x:Name="RingHost" VerticalAlignment="Center" />
            </ScrollViewer>
            <TextBlock x:Name="CollapseHint" Grid.Row="1" Text="◂" FontSize="10" Opacity="0.4"
                       HorizontalAlignment="Center" Margin="0,4,0,0" />
        </Grid>
    </Border>
</Window>
